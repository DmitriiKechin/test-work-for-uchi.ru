import { Button } from '../button/button';
import { Wrapper } from './styles';

export const Title: React.FC = () => {
  return (
    <Wrapper>
      <div>Interview Calendar</div>
      <Button onClick={() => {}} fontSize="1.4em">
        +
      </Button>
    </Wrapper>
  );
};
